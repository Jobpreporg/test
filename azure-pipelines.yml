trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: SSH@0
  inputs:
    sshEndpoint: 'radicalVMSC'
    runOptions: 'inline'
    inline: |
      echo "Uploading files..."
      sudo apt install rsync -y
      rsync -avz $(Build.SourcesDirectory)/ nazeer@40.81.228.8:/var/www/html/
